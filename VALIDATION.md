# 验证记录

日期：2026-09-16。版本：0.1.0。

## 已执行

- `npm run build`：成功。可加载目录 `extension/` 已包含 Manifest V3、阅读页、PDF.js worker、字体、CMap、WASM 资源与第三方许可证。
- `npm test`：10 项通过，覆盖双栏顺序、跨栏标题分区、17pt 窄栏间隙、重复页眉和页码、保守断词、文字片段合并、无文字页警告、跨页来源保留、工具栏入口 URL 处理，以及构建资源完整性。
- 真实材料：[BERT，ACL Anthology N19-1423](https://aclanthology.org/N19-1423/)，16 页。完整提取无解析错误。检查首页标题、Abstract、Introduction 左右栏顺序、跨页段落；修复了最初窄栏间隙导致的左右栏串句，以及首页出版信息混入正文的问题。
- Chrome 中通过本机 HTTP 预览运行与扩展相同的阅读页及 PDF.js worker：完整论文转换成功；检查目录、字号调节、配色切换、原页链接与原页 canvas，未记录到浏览器错误。
- 在浏览器中导出独立 HTML 成功（约 6.9 MB），重新独立打开成功。检查导出：16 个原页、19 张内嵌图像（含 3 张识别出的公式区域）、0 个脚本。无需服务器提供图片或样式。
- 检视了阅读首页和 PDF 原页截图，字体、控件及原页清晰，无明显裁切。

## 未完成的验证，不应误称通过

- 未替用户在 Chrome 中安装该扩展。因此真正的扩展工具栏点击、Chrome 域名授权、file URL 权限和 Manifest V3 环境的完整端到端链路，仍需首次安装后手动验收。入口逻辑已通过模拟 Chrome API 的自动测试。
- 本地文件选择器已打开，但自动设置文件被浏览器控制工具拒绝：ChatGPT 浏览器扩展未允许文件网址访问。本轮改用同一份本机 PDF 的 HTTP 地址完成解析、界面和导出测试。手动选择文件、拖放路径尚未实测。
- OCR 提示通过无文字页数据测试，尚未用多种真实扫描件做兼容性验证。
- 尚未覆盖密码 PDF、所有复杂数学排版、各出版商版式、RTL、超长文档和多个站点的登录态／重定向。

## 首次安装建议验收

1. 在 ACL 打开 N19-1423 PDF，点击工具栏按钮，确认进入阅读页并出现全文。
2. 用“打开 PDF”选择一份本地论文；可选开启文件网址访问后，验证已在 Chrome 中打开的本地 PDF。
3. 点击 Introduction 目录项，再点旁边的原页，确认文字顺序与原页相符；用“返回刚才阅读处”返回。
4. 调整字号和配色；重新打开阅读页确认设置被记住。
5. 保存 HTML，直接双击打开，检查全文及原页。

这些检查说明可运行性，不代表所有论文均能无损自动重排。剩余问题和解析器接口见 `MVP.md`。

## 0.1.1：正文附图修复（2026-09-16）

- 复现旧版：BERT 16 页有 5 个 Figure 图注，但结构输出中 figure 块为 0。此前的“19 张内嵌图像”只有原页和公式，不代表正文附图已实现。
- 根因：文字提取流程未读取绘图操作，阅读页只渲染公式。增加 `figures.js`，同时识别位图变换和矢量绘制区域，将原图裁切插入对应图注位置。合并完整图注并保留图表轴标签；无法定位时明确展示整页回退。
- 回归：修复前真实 PDF 测试 0≠5 失败；修复后 `PAPERFLOW_TEST_PDF=../../work/bert.pdf npm test` 共 15 项全部通过。真实样本未打包，请自行提供公开 BERT PDF 给该环境变量；未提供时只有这项集成测试会跳过。
- 5 张真实图均采用裁图而非整页回退，逐张渲染检测非空像素；补充位图坐标变换、纯矢量绘制、轴标签保留、图内重复文字清理、引用句排除与无边界回退的测试。
- Chrome 阅读页实测 5 张正文图片全部完成加载；检查 Figure 1 和 Figure 5 原图，轴标签和图例可见，完整图注留在附图下方。无浏览器错误。
- 浏览器导出独立 HTML，检查 5 个正文 figure、24 张内嵌图像（16 原页 + 5 附图 + 3 公式）、0 脚本、0 整页附图回退。
- 范围：此次修复优先支持图在图注上方、带 Figure / Fig. / 图编号的版式。无图注、跨页附图、上方图注、图文严重混排仍需原页核对。未改 Chrome 权限，未自动刷新用户已安装扩展。
